server {
    listen 3200;
    root /usr/share/nginx/html;
    index index.html;

    # Serve at both / (local dev) and /review (production behind Caddy)
    location / {
        try_files $uri $uri/ /index.html;
    }
    location /review {
        try_files $uri $uri/ /index.html;
    }
}
