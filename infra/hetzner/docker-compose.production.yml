# Production overrides for Hetzner deployment
# Usage: docker compose -f docker-compose.yml -f infra/hetzner/docker-compose.production.yml up -d
services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
    restart: unless-stopped

  temporal:
    ports:
      - "127.0.0.1:7233:7233"
    restart: unless-stopped

  temporal-ui:
    ports:
      - "127.0.0.1:8233:8080"
    restart: unless-stopped

  hermes-gateway:
    ports:
      - "127.0.0.1:8642:8642"
    restart: unless-stopped

  hast-api:
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped

  hast-worker:
    restart: unless-stopped

  paperclip:
    ports:
      - "127.0.0.1:3100:3100"
    restart: unless-stopped
    environment:
      - PAPERCLIP_PUBLIC_URL=https://lecture.fierro.co.uk
